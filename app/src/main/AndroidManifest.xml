<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pasaporte_vacunacion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pasaporte_Vacunacion">
        <activity android:name=".Activities.PasaporteActivity"></activity>
        <activity
            android:name=".Activities.RecuperarActivity"
            android:theme="@style/Theme.Pasaporte_Vacunacion.NoActionBar" />
        <activity
            android:name=".Activities.RegistroActivity"
            android:theme="@style/Theme.Pasaporte_Vacunacion.NoActionBar" />
        <activity
            android:name=".Activities.LoginActivity"
            android:theme="@style/Theme.Pasaporte_Vacunacion.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>